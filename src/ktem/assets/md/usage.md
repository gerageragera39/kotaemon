# KURAGa usage

Admins configure models, index university documents, and validate retrieval. Guests use Chat against all admin-indexed documents and can read Project Documentation in the app.

Guest users cannot upload files, select a single file, disable document search, or access admin pages.
